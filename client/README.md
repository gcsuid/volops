# VolOps Client

React + TypeScript + Vite frontend for VolOps.

## Commands

```bash
npm run dev
npm run build
npm run lint
npm run preview
```

## Notes

- API requests use `/api`
- local dev proxies `/api` to `http://localhost:3000`
- production assets are emitted to `client/dist` and served by the root Express app
